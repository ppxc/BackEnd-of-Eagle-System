<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.PrplCheckTaskMapper">

    <select id="getAllTasksByDate" resultType="com.example.demo.entity.PrplCheckTask">
        SELECT id, checklongitude, checklatitude, checkdate, checksite
        FROM prplchecktask
        WHERE checkdate = #{dateStr}
    </select>

    <select id="getAllTasks" resultType="com.example.demo.entity.PrplCheckTask">
        SELECT id, checklongitude, checklatitude, checkdate, checksite
        FROM prplchecktask
    </select>

</mapper>
