<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OperationLogMapper">

    <select id="selectByCondition" resultType="com.example.demo.entity.OperationLog">
        SELECT id, username, usercode, roles, operation, create_time as createTime
        FROM sys_operation_log
        WHERE 1=1
        <if test="username != null and username != ''">
            AND username LIKE '%' || #{username} || '%'
        </if>
        <if test="usercode != null and usercode != ''">
            AND usercode = #{usercode}
        </if>
        <if test="startTime != null and startTime != ''">
            AND create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
        <if test="limit != null and offset != null">
            LIMIT #{limit} OFFSET #{offset}
        </if>
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*)
        FROM sys_operation_log
        WHERE 1=1
        <if test="username != null and username != ''">
            AND username LIKE '%' || #{username} || '%'
        </if>
        <if test="usercode != null and usercode != ''">
            AND usercode = #{usercode}
        </if>
        <if test="startTime != null and startTime != ''">
            AND create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND create_time &lt;= #{endTime}
        </if>
    </select>

</mapper>